boto3>=1.34.0
botocore>=1.34.0
requests>=2.32.0
python-dateutil>=2.8.2
pytz>=2024.1
typing_extensions>=4.10.0

# provider.tracing in serverless.yml only traces the Lambda invocation
# boundary; this is what makes DynamoDB/SQS/SNS calls show up as their own
# nodes in the X-Ray service map -- see src/infra/xray_setup.py.
aws-xray-sdk>=2.12.0

# Pinned to a commit, not a tag -- dbnz-io/dredge has no releases/tags yet.
# Replaces the previously vendored, drifted copy at src/dredge/ (Phase 2).
# Pulls in google-cloud-logging/google-auth transitively even though only
# dredge.aws_ir is used here -- accepted tradeoff, see the roadmap.
dredge @ git+https://github.com/dbnz-io/dredge.git@cb678b43e24cfb3235738488338cc9c61493da1a
