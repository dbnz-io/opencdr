{
  "rule_id": "008_lambda_modified",
  "rule_kind": "signal",
  "created_by": "opencdr",
  "description": "Lambda function created or code updated. Attackers use Lambda for persistent backdoors and execution.",
  "enabled": true,
  "severity": "MEDIUM",
  "notify": true,
  "response_module": "",
  "playbook": "Confirm the change was made via an approved deployment pipeline. Review the function code, execution role, and environment variables for signs of backdoor (exfiltration URLs, hardcoded credentials, unexpected IAM role). If unexpected, revert the function version.",
  "version": 1,
  "conditions": [
    { "field": "api.service", "op": "equals", "value": "lambda.amazonaws.com" },
    { "field": "activity_name", "op": "matches", "value": "^(CreateFunction|UpdateFunctionCode)" }
  ]
}
